<nav class="navbar navbar-expand-sm bg-primary navbar-dark">
    <a class="navbar-brand" href="/">PFS Paint Tracker</a>
    <ul class="navbar-nav">
        @foreach (var item in MenuItems)
        {
<li class="nav-item">
    <NavLink class="nav-link" href="@item.HRef" Match="NavLinkMatch.All">
        @item.Caption
    </NavLink>
</li>}
    </ul>
</nav>

@code { [Parameter]
            public mom_application.Models.MenuItem[] MenuItems { get; set; }
    }